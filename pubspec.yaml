name: undangan_wisuda
description: Aplikasi Undangan Wisuda
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.5.6
  
  # State Management
  provider: ^6.1.2
  
  # QR Code
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.0.1
  
  # UI
  google_fonts: ^6.2.1
  fl_chart: ^1.0.0
  
  # Utils
  intl: ^0.20.2
  share_plus: ^11.0.0
  url_launcher: ^6.2.6
  screenshot: ^3.0.0
  path_provider: ^2.1.1
  
  # Forms
  flutter_form_builder: ^10.0.1
  form_builder_validators: ^11.1.2

  # Excel Import
  file_picker: ^10.2.0
  excel: ^4.0.3

  # Location & Maps
  flutter_map: ^8.1.1
  latlong2: ^0.9.0
  
  # UI Components
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
  #   - assets/icons/